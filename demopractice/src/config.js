/*const config = {
    url: "http://localhost:2030/userpractice"  
}

export default config;
*/


// No ports, relative path
const config = {
  url: "/userpractice"   // points to backend context in Tomcat
};

export default config;
