/*const config = {
    url: "http://localhost:2030/userpractice"  
}

export default config;*/


// frontend config for backend
const config = {
    url: "http://localhost:2001/user"  // Spring Boot backend port
}

export default config
